/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getArrayDecoder,
  getArrayEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { SHARKY_PROGRAM_ADDRESS } from "../programs";
import {
  getUpdateIndexDecoder,
  getUpdateIndexEncoder,
  type UpdateIndex,
  type UpdateIndexArgs,
} from "../types";

export const UPDATE_NFT_LIST_DISCRIMINATOR = new Uint8Array([
  215, 13, 25, 187, 11, 93, 34, 143,
]);

export function getUpdateNftListDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    UPDATE_NFT_LIST_DISCRIMINATOR,
  );
}

export type UpdateNftListInstruction<
  TProgram extends string = typeof SHARKY_PROGRAM_ADDRESS,
  TAccountNftList extends string | AccountMeta<string> = string,
  TAccountPayer extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountNftList extends string
        ? WritableAccount<TAccountNftList>
        : TAccountNftList,
      TAccountPayer extends string
        ? ReadonlySignerAccount<TAccountPayer> &
            AccountSignerMeta<TAccountPayer>
        : TAccountPayer,
      ...TRemainingAccounts,
    ]
  >;

export type UpdateNftListInstructionData = {
  discriminator: ReadonlyUint8Array;
  mints: Array<UpdateIndex>;
};

export type UpdateNftListInstructionDataArgs = {
  mints: Array<UpdateIndexArgs>;
};

export function getUpdateNftListInstructionDataEncoder(): Encoder<UpdateNftListInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["mints", getArrayEncoder(getUpdateIndexEncoder())],
    ]),
    (value) => ({ ...value, discriminator: UPDATE_NFT_LIST_DISCRIMINATOR }),
  );
}

export function getUpdateNftListInstructionDataDecoder(): Decoder<UpdateNftListInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["mints", getArrayDecoder(getUpdateIndexDecoder())],
  ]);
}

export function getUpdateNftListInstructionDataCodec(): Codec<
  UpdateNftListInstructionDataArgs,
  UpdateNftListInstructionData
> {
  return combineCodec(
    getUpdateNftListInstructionDataEncoder(),
    getUpdateNftListInstructionDataDecoder(),
  );
}

export type UpdateNftListInput<
  TAccountNftList extends string = string,
  TAccountPayer extends string = string,
> = {
  nftList: Address<TAccountNftList>;
  payer: TransactionSigner<TAccountPayer>;
  mints: UpdateNftListInstructionDataArgs["mints"];
};

export function getUpdateNftListInstruction<
  TAccountNftList extends string,
  TAccountPayer extends string,
  TProgramAddress extends Address = typeof SHARKY_PROGRAM_ADDRESS,
>(
  input: UpdateNftListInput<TAccountNftList, TAccountPayer>,
  config?: { programAddress?: TProgramAddress },
): UpdateNftListInstruction<TProgramAddress, TAccountNftList, TAccountPayer> {
  // Program address.
  const programAddress = config?.programAddress ?? SHARKY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    nftList: { value: input.nftList ?? null, isWritable: true },
    payer: { value: input.payer ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("nftList", accounts.nftList),
      getAccountMeta("payer", accounts.payer),
    ],
    data: getUpdateNftListInstructionDataEncoder().encode(
      args as UpdateNftListInstructionDataArgs,
    ),
    programAddress,
  } as UpdateNftListInstruction<
    TProgramAddress,
    TAccountNftList,
    TAccountPayer
  >);
}

export type ParsedUpdateNftListInstruction<
  TProgram extends string = typeof SHARKY_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    nftList: TAccountMetas[0];
    payer: TAccountMetas[1];
  };
  data: UpdateNftListInstructionData;
};

export function parseUpdateNftListInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedUpdateNftListInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 2,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { nftList: getNextAccount(), payer: getNextAccount() },
    data: getUpdateNftListInstructionDataDecoder().decode(instruction.data),
  };
}
