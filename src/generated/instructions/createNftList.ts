/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getUtf8Decoder,
  getUtf8Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { SHARKY_PROGRAM_ADDRESS } from "../programs";

export const CREATE_NFT_LIST_DISCRIMINATOR = new Uint8Array([
  243, 38, 198, 76, 172, 64, 127, 24,
]);

export function getCreateNftListDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    CREATE_NFT_LIST_DISCRIMINATOR,
  );
}

export type CreateNftListInstruction<
  TProgram extends string = typeof SHARKY_PROGRAM_ADDRESS,
  TAccountNftList extends string | AccountMeta<string> = string,
  TAccountPayer extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountNftList extends string
        ? WritableAccount<TAccountNftList>
        : TAccountNftList,
      TAccountPayer extends string
        ? ReadonlySignerAccount<TAccountPayer> &
            AccountSignerMeta<TAccountPayer>
        : TAccountPayer,
      ...TRemainingAccounts,
    ]
  >;

export type CreateNftListInstructionData = {
  discriminator: ReadonlyUint8Array;
  collectionName: string;
};

export type CreateNftListInstructionDataArgs = { collectionName: string };

export function getCreateNftListInstructionDataEncoder(): Encoder<CreateNftListInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      [
        "collectionName",
        addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder()),
      ],
    ]),
    (value) => ({ ...value, discriminator: CREATE_NFT_LIST_DISCRIMINATOR }),
  );
}

export function getCreateNftListInstructionDataDecoder(): Decoder<CreateNftListInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["collectionName", addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
  ]);
}

export function getCreateNftListInstructionDataCodec(): Codec<
  CreateNftListInstructionDataArgs,
  CreateNftListInstructionData
> {
  return combineCodec(
    getCreateNftListInstructionDataEncoder(),
    getCreateNftListInstructionDataDecoder(),
  );
}

export type CreateNftListInput<
  TAccountNftList extends string = string,
  TAccountPayer extends string = string,
> = {
  nftList: Address<TAccountNftList>;
  payer: TransactionSigner<TAccountPayer>;
  collectionName: CreateNftListInstructionDataArgs["collectionName"];
};

export function getCreateNftListInstruction<
  TAccountNftList extends string,
  TAccountPayer extends string,
  TProgramAddress extends Address = typeof SHARKY_PROGRAM_ADDRESS,
>(
  input: CreateNftListInput<TAccountNftList, TAccountPayer>,
  config?: { programAddress?: TProgramAddress },
): CreateNftListInstruction<TProgramAddress, TAccountNftList, TAccountPayer> {
  // Program address.
  const programAddress = config?.programAddress ?? SHARKY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    nftList: { value: input.nftList ?? null, isWritable: true },
    payer: { value: input.payer ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("nftList", accounts.nftList),
      getAccountMeta("payer", accounts.payer),
    ],
    data: getCreateNftListInstructionDataEncoder().encode(
      args as CreateNftListInstructionDataArgs,
    ),
    programAddress,
  } as CreateNftListInstruction<
    TProgramAddress,
    TAccountNftList,
    TAccountPayer
  >);
}

export type ParsedCreateNftListInstruction<
  TProgram extends string = typeof SHARKY_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    nftList: TAccountMetas[0];
    payer: TAccountMetas[1];
  };
  data: CreateNftListInstructionData;
};

export function parseCreateNftListInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedCreateNftListInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 2,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { nftList: getNextAccount(), payer: getNextAccount() },
    data: getCreateNftListInstructionDataDecoder().decode(instruction.data),
  };
}
